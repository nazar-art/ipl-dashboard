spring:
  application:
    name: 'ipl-dashboard'
  datasource:
    url: jdbc:h2:mem:ipl_db
    driverClassName: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    open-in-view: false

csv-dataset: match-data.csv